# Reference data lives here — e.g. crop coefficient (Kc) tables,
# soil water-holding-capacity tables, NPK baseline tables.
#
# TODO: as you fill in the lookup tables in services/evapotranspiration.py,
# services/irrigation_engine.py, and services/fertilizer_engine.py, consider
# moving large tables into dedicated files here (e.g. crop_coefficients.py,
# soil_properties.py) so the service files stay focused on logic rather
# than data.
